FROM rasa/rasa-sdk:latest

USER root
WORKDIR /app
COPY actions /app/actions
COPY requirements-actions.txt /app
RUN pip install -r requirements-actions.txt

USER 1001

ENTRYPOINT ["rasa"]
CMD ["run", "actions", "--port", "${PORT_ACTIONS:-5055}"]